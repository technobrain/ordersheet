<%= form_for(position_master) do |f| %>
  <% if position_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position_master.errors.count, "error") %> prohibited this position_master from being saved:</h2>

      <ul>
      <% position_master.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :position_name %><br />
    <%= f.text_field :position_name %>
  </div>
  <div class="field">
    <%= f.label :position_expenses %><br />
    <%= f.number_field :position_expenses %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
