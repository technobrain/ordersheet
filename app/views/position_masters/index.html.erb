<%= render '/layouts/header' %>
<h2>職制マスタ　一覧</h2>

<table id="default">
  <thead>
    <tr>
      <th>職制ID</th>
      <th>職制名</th>
      <th>経費</th>
      <th colspan=3></th>
    </tr>
  </thead>

  <tbody>
    <% @position_masters.each do |position_master| %>
    <tr>
      <td><%= position_master.id %></td>
      <td><%= position_master.position_name %></td>
      <td><%= position_master.position_expenses %></td>
      <td><%= link_to '照会', position_master %></td>
      <td><%= link_to '更新', edit_position_master_path(position_master) %></td>
      <td><%= link_to '削除', position_master, method: :delete, data: { confirm: '削除してよろしいですか？' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br />

<%= link_to '新規登録', new_position_master_path %>
